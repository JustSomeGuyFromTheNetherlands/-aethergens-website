<%- include('partials/head', { pageTitle: 'Register', pageDescription: 'Create an account to join our community' }) %>
<% const currentPage = 'register'; %>
<%- include('partials/navbar') %>

<section class="py-16">
    <div class="max-w-md mx-auto px-4">
        <div class="bg-slate-800/60 backdrop-blur-xl rounded-2xl p-8 border border-purple-500/20">
            <div class="text-center mb-8">
                <i class="fas fa-user-plus text-4xl text-purple-400 mb-4"></i>
                <h1 class="text-3xl font-bold text-white mb-2">Create Account</h1>
                <p class="text-slate-400">Join our community</p>
            </div>

            <% if (success && message) { %>
                <div class="bg-green-900/50 border border-green-500 rounded-lg p-4 mb-6 text-green-300">
                    <i class="fas fa-check-circle mr-2"></i><%= message %>
                    <div class="mt-4">
                        <a href="/admin/login" class="bg-green-600 hover:bg-green-700 px-4 py-2 rounded text-white font-medium">
                            Go to Login
                        </a>
                    </div>
                </div>
            <% } else if (error) { %>
                <div class="bg-red-900/50 border border-red-500 rounded-lg p-4 mb-6 text-red-300">
                    <i class="fas fa-exclamation-triangle mr-2"></i>
                    <div><%- error %></div>
                </div>
            <% } %>

            <% if (!success) { %>
                <form method="POST" class="space-y-6">
                    <div>
                        <label class="block text-slate-300 mb-2 text-sm font-medium">Username</label>
                        <input type="text" name="username" required
                               class="w-full bg-slate-900 border border-slate-700 rounded px-4 py-3 text-white focus:border-purple-500 focus:outline-none transition"
                               placeholder="Choose a username">
                    </div>

                    <div>
                        <label class="block text-slate-300 mb-2 text-sm font-medium">Email</label>
                        <input type="email" name="email" required
                               class="w-full bg-slate-900 border border-slate-700 rounded px-4 py-3 text-white focus:border-purple-500 focus:outline-none transition"
                               placeholder="your@email.com">
                    </div>

                    <div>
                        <label class="block text-slate-300 mb-2 text-sm font-medium">Minecraft Username</label>
                        <input type="text" name="minecraft_username" required
                               class="w-full bg-slate-900 border border-slate-700 rounded px-4 py-3 text-white focus:border-purple-500 focus:outline-none transition"
                               placeholder="Your Minecraft username">
                        <p class="text-xs text-slate-500 mt-1">Used to link your account to in-game data</p>
                    </div>

                    <div>
                        <label class="block text-slate-300 mb-2 text-sm font-medium">Password</label>
                        <input type="password" name="password" required
                               class="w-full bg-slate-900 border border-slate-700 rounded px-4 py-3 text-white focus:border-purple-500 focus:outline-none transition"
                               placeholder="Create a password">
                    </div>

                    <div>
                        <label class="block text-slate-300 mb-2 text-sm font-medium">Confirm Password</label>
                        <input type="password" name="confirm_password" required
                               class="w-full bg-slate-900 border border-slate-700 rounded px-4 py-3 text-white focus:border-purple-500 focus:outline-none transition"
                               placeholder="Confirm your password">
                    </div>

                    <button type="submit" class="w-full bg-gradient-to-r from-purple-600 to-pink-600 hover:from-purple-700 hover:to-pink-700 px-6 py-3 rounded-lg font-semibold text-white transition-all">
                        <i class="fas fa-user-plus mr-2"></i>Create Account
                    </button>
                </form>

                <div class="mt-6 text-center">
                    <p class="text-slate-400">
                        Already have an account?
                        <a href="/admin/login" class="text-purple-400 hover:text-purple-300 font-medium">Sign in</a>
                    </p>
                </div>
            <% } %>

            <!-- Benefits -->
            <div class="mt-8 pt-6 border-t border-slate-700">
                <h3 class="text-lg font-semibold text-white mb-4">Why register?</h3>
                <div class="space-y-3">
                    <div class="flex items-center text-sm text-slate-300">
                        <i class="fas fa-check text-green-400 mr-3"></i>
                        Access exclusive features
                    </div>
                    <div class="flex items-center text-sm text-slate-300">
                        <i class="fas fa-check text-green-400 mr-3"></i>
                        Track your playtime and stats
                    </div>
                    <div class="flex items-center text-sm text-slate-300">
                        <i class="fas fa-check text-green-400 mr-3"></i>
                        Participate in giveaways
                    </div>
                    <div class="flex items-center text-sm text-slate-300">
                        <i class="fas fa-check text-green-400 mr-3"></i>
                        Get notified of updates
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>

<%- include('partials/footer') %>
</body>
</html>
