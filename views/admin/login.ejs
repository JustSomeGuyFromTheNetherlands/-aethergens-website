<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login - Minecraft CMS</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css">
    <style>
        body { background: linear-gradient(135deg, #0f172a 0%, #1e293b 100%); }
    </style>
</head>
<body class="min-h-screen flex items-center justify-center">
    <div class="bg-slate-800 rounded-lg p-8 border border-slate-700 max-w-md w-full">
        <div class="text-center mb-6">
            <i class="fas fa-cube text-5xl text-blue-400 mb-4"></i>
            <h1 class="text-2xl font-bold text-white">Minecraft CMS</h1>
            <p class="text-slate-400 mt-2">Admin Login</p>
        </div>
        
        <% if (error) { %>
            <div class="bg-red-900/50 border border-red-500 rounded p-3 mb-4 text-red-300">
                <%= error %>
            </div>
        <% } %>
        
        <form method="POST" class="space-y-4">
            <div>
                <label class="block text-slate-300 mb-2">Username</label>
                <input type="text" name="username" required autofocus class="w-full bg-slate-900 border border-slate-700 rounded px-4 py-2 text-white">
            </div>
            <div>
                <label class="block text-slate-300 mb-2">Password</label>
                <input type="password" name="password" required class="w-full bg-slate-900 border border-slate-700 rounded px-4 py-2 text-white">
            </div>
            <button type="submit" class="w-full bg-blue-600 hover:bg-blue-700 px-4 py-2 rounded text-white font-semibold">
                <i class="fas fa-sign-in-alt mr-2"></i>Login
            </button>
        </form>
    </div>
</body>
</html>

