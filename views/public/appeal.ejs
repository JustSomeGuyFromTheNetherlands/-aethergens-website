<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Ban Appeal - <%= serverSettings.server_name || 'Minecraft Server' %></title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css">
    <style>
        body { background: #0f172a; }
    </style>
</head>
<body class="text-white min-h-screen flex items-center justify-center py-12 px-4">
    <div class="max-w-2xl w-full">
        <div class="text-center mb-8">
            <h1 class="text-5xl font-black mb-4 bg-gradient-to-r from-blue-400 to-cyan-400 bg-clip-text text-transparent">Ban Appeal</h1>
            <p class="text-slate-400 text-lg">Submit an appeal if you believe you were banned unfairly</p>
        </div>
        
        <% if (message) { %>
            <div class="bg-green-900/50 border border-green-500 rounded-lg p-4 mb-6 text-green-300">
                <%= message %>
            </div>
        <% } %>
        
        <% if (error) { %>
            <div class="bg-red-900/50 border border-red-500 rounded-lg p-4 mb-6 text-red-300">
                <%= error %>
            </div>
        <% } %>
        
        <% if (banInfo) { %>
            <div class="bg-slate-800 rounded-lg p-6 mb-6 border border-slate-700">
                <h3 class="text-xl font-bold mb-4">Ban Information</h3>
                <p><strong>Reason:</strong> <%= banInfo.reason %></p>
                <p><strong>Banned By:</strong> <%= banInfo.banned_by %></p>
                <p><strong>Date:</strong> <%= new Date(banInfo.ban_date).toLocaleDateString() %></p>
            </div>
        <% } %>
        
        <form method="POST" class="bg-slate-800 rounded-lg p-8 border border-slate-700">
            <div class="space-y-4">
                <div>
                    <label class="block text-slate-300 mb-2">Minecraft Username *</label>
                    <input type="text" name="username" required value="<%= banInfo ? banInfo.player_username : '' %>" class="w-full bg-slate-900 border border-slate-700 rounded px-4 py-2">
                </div>
                <div>
                    <label class="block text-slate-300 mb-2">Email Address *</label>
                    <input type="email" name="email" required class="w-full bg-slate-900 border border-slate-700 rounded px-4 py-2">
                </div>
                <div>
                    <label class="block text-slate-300 mb-2">Appeal Message *</label>
                    <textarea name="appeal_text" required rows="6" class="w-full bg-slate-900 border border-slate-700 rounded px-4 py-2"></textarea>
                </div>
                <button type="submit" class="w-full bg-blue-600 hover:bg-blue-700 px-6 py-3 rounded font-semibold">
                    <i class="fas fa-paper-plane mr-2"></i>Submit Appeal
                </button>
            </div>
        </form>
    </div>
</body>
</html>

