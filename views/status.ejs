<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Server Status - <%= serverSettings.server_name %></title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css">
    <style>
        body { background: linear-gradient(135deg, #0a0e27 0%, #1a1f3a 50%, #2d1b3d 100%); min-height: 100vh; }
        .card {
            background: rgba(30, 41, 59, 0.6);
            backdrop-filter: blur(12px);
            border: 1px solid rgba(139, 92, 246, 0.2);
        }
    </style>
</head>
<body class="text-white">
    <nav class="bg-slate-900/80 backdrop-blur-md border-b border-purple-500/20 sticky top-0 z-50 shadow-lg">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="flex justify-between h-20 items-center">
                <div class="flex items-center space-x-2">
                    <div class="w-10 h-10 bg-gradient-to-br from-purple-500 to-pink-500 rounded-lg flex items-center justify-center">
                        <i class="fas fa-cube text-white text-xl"></i>
                    </div>
                    <h1 class="text-2xl font-bold bg-gradient-to-r from-purple-400 to-pink-400 bg-clip-text text-transparent"><%= serverSettings.server_name %></h1>
                </div>
                <a href="/" class="text-slate-400 hover:text-white transition">Back to Home</a>
            </div>
        </div>
    </nav>

    <div class="max-w-7xl mx-auto px-4 py-12">
        <div class="text-center mb-12">
            <h1 class="text-5xl font-black mb-4">Server Status</h1>
            <p class="text-xl text-slate-400">Current status and incident history</p>
        </div>

        <!-- Current Server Status -->
        <div class="card rounded-xl p-6 mb-8">
            <h2 class="text-2xl font-bold mb-4">Current Status</h2>
            <div class="grid grid-cols-1 md:grid-cols-3 gap-4">
                <div class="bg-slate-800/50 rounded-lg p-4">
                    <p class="text-slate-400 text-sm mb-2">Server</p>
                    <% if (serverStatus && serverStatus.online) { %>
                        <p class="text-2xl font-bold text-green-400 flex items-center">
                            <span class="w-3 h-3 bg-green-400 rounded-full mr-2 animate-pulse"></span>Online
                        </p>
                        <p class="text-sm text-slate-400 mt-2">
                            <%= serverStatus.players || 0 %>/<%= serverStatus.max_players || 0 %> players
                        </p>
                    <% } else { %>
                        <p class="text-2xl font-bold text-red-400 flex items-center">
                            <span class="w-3 h-3 bg-red-400 rounded-full mr-2"></span>Offline
                        </p>
                    <% } %>
                </div>
                <div class="bg-slate-800/50 rounded-lg p-4">
                    <p class="text-slate-400 text-sm mb-2">Active Incidents</p>
                    <p class="text-2xl font-bold <%= activeIncidents.length > 0 ? 'text-yellow-400' : 'text-green-400' %>">
                        <%= activeIncidents.length %>
                    </p>
                </div>
                <div class="bg-slate-800/50 rounded-lg p-4">
                    <p class="text-slate-400 text-sm mb-2">Uptime</p>
                    <p class="text-2xl font-bold text-green-400">99.9%</p>
                    <p class="text-sm text-slate-400 mt-2">Last 30 days</p>
                </div>
            </div>
        </div>

        <!-- Active Incidents -->
        <% if (activeIncidents.length > 0) { %>
            <div class="mb-8">
                <h2 class="text-3xl font-bold mb-4">Active Incidents</h2>
                <div class="space-y-4">
                    <% activeIncidents.forEach(incident => { %>
                        <%
                        const typeClasses = {
                            'announcement': 'border-blue-500/50 bg-blue-900/30',
                            'planned': 'border-green-500/50 bg-green-900/30',
                            'downtime': 'border-yellow-500/50 bg-yellow-900/30',
                            'unexpected': 'border-red-500/50 bg-red-900/30',
                            'maintenance': 'border-purple-500/50 bg-purple-900/30'
                        };
                        const typeIcons = {
                            'announcement': 'fa-bullhorn text-blue-400',
                            'planned': 'fa-calendar-alt text-green-400',
                            'downtime': 'fa-clock text-yellow-400',
                            'unexpected': 'fa-exclamation-triangle text-red-400',
                            'maintenance': 'fa-tools text-purple-400'
                        };
                        const class_ = typeClasses[incident.type] || 'border-slate-500/50 bg-slate-800/30';
                        const icon = typeIcons[incident.type] || 'fa-info-circle text-slate-400';
                        const priorityColors = {
                            'urgent': 'bg-red-600 text-white',
                            'high': 'bg-orange-600 text-white',
                            'normal': 'bg-blue-600 text-white',
                            'low': 'bg-slate-600 text-white'
                        };
                        %>
                        <div class="<%= class_ %> border-2 rounded-xl p-6">
                            <div class="flex items-start gap-4">
                                <div class="w-12 h-12 bg-white/10 rounded-lg flex items-center justify-center flex-shrink-0">
                                    <i class="fas <%= icon %> text-2xl"></i>
                                </div>
                                <div class="flex-1">
                                    <div class="flex items-center justify-between mb-2">
                                        <h3 class="text-xl font-bold"><%= incident.title %></h3>
                                        <span class="px-3 py-1 rounded text-xs font-semibold <%= priorityColors[incident.priority] || 'bg-slate-600 text-white' %>">
                                            <%= (incident.priority || 'normal').toUpperCase() %>
                                        </span>
                                    </div>
                                    <p class="text-slate-300 mb-3"><%= incident.message.replace(/\n/g, '<br>') %></p>
                                    <div class="flex items-center gap-4 text-sm text-slate-400">
                                        <% if (incident.start_date) { %>
                                            <span><i class="fas fa-calendar-check mr-1"></i>Started: <%= new Date(incident.start_date).toLocaleString('en-US', { month: 'short', day: 'numeric', year: 'numeric', hour: '2-digit', minute: '2-digit' }) %></span>
                                        <% } %>
                                        <% if (incident.end_date) { %>
                                            <span><i class="fas fa-calendar-times mr-1"></i>Expected End: <%= new Date(incident.end_date).toLocaleString('en-US', { month: 'short', day: 'numeric', year: 'numeric', hour: '2-digit', minute: '2-digit' }) %></span>
                                        <% } %>
                                        <span><i class="fas fa-user mr-1"></i>Posted by: <%= incident.created_by || 'Admin' %></span>
                                    </div>
                                </div>
                            </div>
                        </div>
                    <% }); %>
                </div>
            </div>
        <% } %>

        <!-- Past Incidents -->
        <div>
            <h2 class="text-3xl font-bold mb-4">Past Incidents</h2>
            <% if (pastIncidents.length === 0) { %>
                <div class="card rounded-xl p-12 text-center">
                    <i class="fas fa-check-circle text-6xl text-green-500 mb-4"></i>
                    <p class="text-xl text-slate-400">No past incidents recorded</p>
                </div>
            <% } else { %>
                <div class="space-y-3">
                    <% pastIncidents.forEach(incident => { %>
                        <%
                        const typeClasses = {
                            'announcement': 'border-blue-500/30 bg-blue-900/20',
                            'planned': 'border-green-500/30 bg-green-900/20',
                            'downtime': 'border-yellow-500/30 bg-yellow-900/20',
                            'unexpected': 'border-red-500/30 bg-red-900/20',
                            'maintenance': 'border-purple-500/30 bg-purple-900/20'
                        };
                        const class_ = typeClasses[incident.type] || 'border-slate-500/30 bg-slate-800/20';
                        %>
                        <div class="<%= class_ %> border rounded-lg p-4 hover:bg-opacity-50 transition">
                            <div class="flex items-start justify-between">
                                <div class="flex-1">
                                    <div class="flex items-center gap-3 mb-1">
                                        <h4 class="text-white font-semibold"><%= incident.title %></h4>
                                        <span class="px-2 py-0.5 rounded text-xs bg-slate-700 text-slate-300">
                                            <%= (incident.type || 'announcement').charAt(0).toUpperCase() + (incident.type || 'announcement').slice(1) %>
                                        </span>
                                        <% if (!incident.active) { %>
                                            <span class="px-2 py-0.5 rounded text-xs bg-slate-700 text-slate-400">Resolved</span>
                                        <% } %>
                                    </div>
                                    <p class="text-slate-400 text-sm mb-2"><%= incident.message %></p>
                                    <div class="flex items-center gap-4 text-xs text-slate-500">
                                        <% if (incident.start_date) { %>
                                            <span><%= new Date(incident.start_date).toLocaleString('en-US', { month: 'short', day: 'numeric', year: 'numeric', hour: '2-digit', minute: '2-digit' }) %></span>
                                        <% } %>
                                        <% if (incident.end_date) { %>
                                            <span>→ <%= new Date(incident.end_date).toLocaleString('en-US', { month: 'short', day: 'numeric', year: 'numeric', hour: '2-digit', minute: '2-digit' }) %></span>
                                        <% } %>
                                        <span>• Posted: <%= new Date(incident.created_at).toLocaleDateString('en-US', { month: 'short', day: 'numeric', year: 'numeric' }) %></span>
                                    </div>
                                </div>
                            </div>
                        </div>
                    <% }); %>
                </div>
            <% } %>
        </div>
    </div>
</body>
</html>


