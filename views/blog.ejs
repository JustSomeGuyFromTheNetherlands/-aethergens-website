<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= pageTitle %> - <%= serverSettings ? serverSettings.server_name : 'Server' %></title>
  <meta name="description" content="<%= pageDescription %>">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
  <style>
    :root {
      --primary-color: #667eea;
      --secondary-color: #764ba2;
      --accent-color: #f093fb;
      --success-color: #10b981;
      --warning-color: #f59e0b;
      --danger-color: #ef4444;
      --dark-bg: #0f172a;
      --card-bg: #1e293b;
      --text-color: #e2e8f0;
    }

    body {
      background: linear-gradient(135deg, var(--dark-bg) 0%, #1a2332 100%);
      color: var(--text-color);
      font-family: 'Inter', sans-serif;
      min-height: 100vh;
    }

    .navbar {
      background: rgba(15, 23, 42, 0.95) !important;
      backdrop-filter: blur(10px);
      border-bottom: 1px solid rgba(255, 255, 255, 0.1);
    }

    .card {
      background: var(--card-bg);
      border: 1px solid rgba(255, 255, 255, 0.1);
      border-radius: 12px;
      box-shadow: 0 8px 32px rgba(0, 0, 0, 0.3);
      transition: transform 0.2s ease, box-shadow 0.2s ease;
    }

    .card:hover {
      transform: translateY(-2px);
      box-shadow: 0 12px 40px rgba(0, 0, 0, 0.4);
    }

    .post-card .card-body {
      padding: 1.5rem;
    }

    .post-image {
      height: 200px;
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      border-radius: 8px 8px 0 0;
    }

    .post-meta {
      font-size: 0.875rem;
      color: rgba(255, 255, 255, 0.6);
      margin-bottom: 0.5rem;
    }

    .post-title {
      color: var(--text-color);
      text-decoration: none;
      font-weight: 600;
      margin-bottom: 0.5rem;
      display: block;
    }

    .post-title:hover {
      color: var(--primary-color);
    }

    .post-excerpt {
      color: rgba(255, 255, 255, 0.8);
      line-height: 1.5;
    }

    .category-badge {
      display: inline-block;
      padding: 0.25rem 0.5rem;
      border-radius: 6px;
      font-size: 0.75rem;
      font-weight: 600;
      text-transform: uppercase;
      letter-spacing: 0.05em;
    }

    .tag-badge {
      display: inline-block;
      padding: 0.2rem 0.4rem;
      border-radius: 4px;
      font-size: 0.7rem;
      margin: 0.1rem;
      text-decoration: none;
    }

    .tag-badge:hover {
      opacity: 0.8;
    }

    .sidebar-card {
      background: var(--card-bg);
      border: 1px solid rgba(255, 255, 255, 0.1);
      border-radius: 12px;
      margin-bottom: 1.5rem;
    }

    .sidebar-title {
      color: var(--primary-color);
      font-weight: 600;
      margin-bottom: 1rem;
      padding-bottom: 0.5rem;
      border-bottom: 1px solid rgba(255, 255, 255, 0.1);
    }

    .recent-post-link {
      color: var(--text-color);
      text-decoration: none;
      display: block;
      padding: 0.5rem 0;
      border-bottom: 1px solid rgba(255, 255, 255, 0.05);
    }

    .recent-post-link:hover {
      color: var(--primary-color);
    }

    .recent-post-title {
      font-size: 0.9rem;
      font-weight: 500;
      margin-bottom: 0.25rem;
    }

    .recent-post-date {
      font-size: 0.8rem;
      color: rgba(255, 255, 255, 0.6);
    }

    .filter-link {
      color: var(--text-color);
      text-decoration: none;
      display: block;
      padding: 0.5rem 0;
      border-radius: 6px;
      transition: all 0.2s ease;
    }

    .filter-link:hover {
      background: rgba(255, 255, 255, 0.1);
      color: var(--primary-color);
    }

    .filter-link.active {
      background: var(--primary-color);
      color: white;
    }

    .pagination .page-link {
      background: var(--card-bg);
      border-color: rgba(255, 255, 255, 0.1);
      color: var(--text-color);
    }

    .pagination .page-link:hover {
      background: rgba(255, 255, 255, 0.1);
      border-color: var(--primary-color);
      color: var(--primary-color);
    }

    .pagination .page-item.active .page-link {
      background: var(--primary-color);
      border-color: var(--primary-color);
    }

    .empty-state {
      text-align: center;
      padding: 3rem 1rem;
    }

    .empty-state i {
      font-size: 3rem;
      color: rgba(255, 255, 255, 0.3);
      margin-bottom: 1rem;
    }
  </style>
</head>
<body>
  <!-- Navigation -->
  <nav class="navbar navbar-expand-lg navbar-dark">
    <div class="container">
      <a class="navbar-brand" href="/">
        <i class="fas fa-server me-2"></i>
        <%= serverSettings ? serverSettings.server_name : 'Server' %>
      </a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav ms-auto">
          <li class="nav-item">
            <a class="nav-link" href="/">Home</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/status">Status</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/leaderboard">Leaderboard</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/shop">Shop</a>
          </li>
          <li class="nav-item">
            <a class="nav-link active" href="/blog">Blog</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/changelog">Changelog</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/vote">Vote</a>
          </li>
        </ul>
      </div>
    </div>
  </nav>

  <!-- Hero Section -->
  <section class="py-5">
    <div class="container">
      <div class="row justify-content-center text-center mb-5">
        <div class="col-lg-8">
          <h1 class="display-4 fw-bold mb-3">
            <i class="fas fa-blog text-primary me-3"></i>
            <%= pageTitle %>
          </h1>
          <p class="lead text-muted mb-4">
            <%= pageDescription %>
          </p>
        </div>
      </div>
    </div>
  </section>

  <!-- Main Content -->
  <section class="pb-5">
    <div class="container">
      <div class="row">
        <!-- Main Content -->
        <div class="col-lg-8">
          <!-- Posts Grid -->
          <% if (posts.length === 0) { %>
            <div class="empty-state">
              <i class="fas fa-newspaper"></i>
              <h3 class="text-muted">No posts found</h3>
              <p class="text-muted">
                <% if (filters.category || filters.tag) { %>
                  No posts found matching your filters.
                  <a href="/blog" class="text-primary">Clear filters</a>
                <% } else { %>
                  Check back later for new content!
                <% } %>
              </p>
            </div>
          <% } else { %>
            <div class="row g-4">
              <% posts.forEach(post => { %>
                <div class="col-md-6">
                  <div class="card post-card h-100">
                    <% if (post.featured_image) { %>
                      <div class="post-image" style="background-image: url('<%= post.featured_image %>')"></div>
                    <% } %>
                    <div class="card-body d-flex flex-column">
                      <div class="mb-2">
                        <% if (post.category_name) { %>
                          <span class="category-badge" style="background-color: <%= post.category_color %>20; color: <%= post.category_color %>;">
                            <i class="fas fa-tag me-1"></i><%= post.category_name %>
                          </span>
                        <% } %>
                      </div>

                      <h5 class="card-title">
                        <a href="/blog/<%= post.slug %>" class="post-title"><%= post.title %></a>
                      </h5>

                      <% if (post.excerpt) { %>
                        <p class="post-excerpt flex-grow-1"><%= post.excerpt %></p>
                      <% } %>

                      <div class="post-meta mt-auto">
                        <div class="d-flex justify-content-between align-items-center">
                          <small>
                            <i class="fas fa-user me-1"></i><%= post.author_name || 'Anonymous' %>
                            <i class="fas fa-calendar ms-3 me-1"></i><%= new Date(post.published_at || post.created_at).toLocaleDateString() %>
                          </small>
                          <small>
                            <i class="fas fa-eye me-1"></i><%= post.views || 0 %>
                            <i class="fas fa-comment ms-2 me-1"></i><%= post.comment_count || 0 %>
                          </small>
                        </div>
                      </div>
                    </div>
                  </div>
                </div>
              <% }); %>
            </div>

            <!-- Pagination -->
            <% if (totalPages > 1) { %>
              <nav aria-label="Blog pagination" class="mt-5">
                <ul class="pagination justify-content-center">
                  <% if (currentPage > 1) { %>
                    <li class="page-item">
                      <a class="page-link" href="?page=<%= currentPage - 1 %><%= filters.category ? `&category=${filters.category}` : '' %><%= filters.tag ? `&tag=${filters.tag}` : '' %>">
                        <i class="fas fa-chevron-left"></i> Previous
                      </a>
                    </li>
                  <% } %>

                  <% for (let i = Math.max(1, currentPage - 2); i <= Math.min(totalPages, currentPage + 2); i++) { %>
                    <li class="page-item <%= i === currentPage ? 'active' : '' %>">
                      <a class="page-link" href="?page=<%= i %><%= filters.category ? `&category=${filters.category}` : '' %><%= filters.tag ? `&tag=${filters.tag}` : '' %>"><%= i %></a>
                    </li>
                  <% } %>

                  <% if (currentPage < totalPages) { %>
                    <li class="page-item">
                      <a class="page-link" href="?page=<%= currentPage + 1 %><%= filters.category ? `&category=${filters.category}` : '' %><%= filters.tag ? `&tag=${filters.tag}` : '' %>">
                        Next <i class="fas fa-chevron-right"></i>
                      </a>
                    </li>
                  <% } %>
                </ul>
              </nav>
            <% } %>
          <% } %>
        </div>

        <!-- Sidebar -->
        <div class="col-lg-4">
          <!-- Categories -->
          <div class="sidebar-card">
            <div class="card-body">
              <h5 class="sidebar-title">
                <i class="fas fa-folder me-2"></i>Categories
              </h5>
              <% categories.forEach(category => { %>
                <a href="<%= filters.category == category.id ? '/blog' : `/blog?category=${category.id}` %>"
                   class="filter-link <%= filters.category == category.id ? 'active' : '' %>">
                  <i class="fas fa-tag me-2" style="color: <%= category.color %>"></i>
                  <%= category.name %>
                </a>
              <% }); %>
              <% if (filters.category) { %>
                <a href="/blog" class="filter-link text-muted mt-2">
                  <i class="fas fa-times me-2"></i>Clear filter
                </a>
              <% } %>
            </div>
          </div>

          <!-- Popular Tags -->
          <% if (tags.length > 0) { %>
            <div class="sidebar-card">
              <div class="card-body">
                <h5 class="sidebar-title">
                  <i class="fas fa-hashtag me-2"></i>Popular Tags
                </h5>
                <div class="d-flex flex-wrap">
                  <% tags.slice(0, 15).forEach(tag => { %>
                    <a href="<%= filters.tag == tag.id ? '/blog' : `/blog?tag=${tag.id}` %>"
                       class="tag-badge"
                       style="background-color: <%= tag.color %>20; color: <%= tag.color %>; border: 1px solid <%= tag.color %>40;">
                      #<%= tag.name %>
                    </a>
                  <% }); %>
                </div>
                <% if (filters.tag) { %>
                  <a href="/blog" class="filter-link text-muted mt-2">
                    <i class="fas fa-times me-2"></i>Clear tag filter
                  </a>
                <% } %>
              </div>
            </div>
          <% } %>

          <!-- Recent Posts -->
          <% if (recentPosts.length > 0) { %>
            <div class="sidebar-card">
              <div class="card-body">
                <h5 class="sidebar-title">
                  <i class="fas fa-clock me-2"></i>Recent Posts
                </h5>
                <% recentPosts.forEach(post => { %>
                  <a href="/blog/<%= post.slug %>" class="recent-post-link">
                    <div class="recent-post-title"><%= post.title %></div>
                    <div class="recent-post-date">
                      <%= new Date(post.published_at || post.created_at).toLocaleDateString() %>
                    </div>
                  </a>
                <% }); %>
              </div>
            </div>
          <% } %>
        </div>
      </div>
    </div>
  </section>

  <!-- Footer -->
  <footer class="bg-dark text-light py-4 mt-5">
    <div class="container">
      <div class="row">
        <div class="col-md-6">
          <p class="mb-0">
            <i class="fas fa-server me-2"></i>
            <%= serverSettings ? serverSettings.server_name : 'Server' %>
          </p>
        </div>
        <div class="col-md-6 text-end">
          <p class="mb-0">
            <i class="fas fa-code me-2"></i>
            Powered by AetherGens CMS
          </p>
        </div>
      </div>
    </div>
  </footer>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
